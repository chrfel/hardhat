import React from "react";
import { InputText } from "primereact/inputtext";
import { Button } from 'primereact/button';

export function Webshop({ transferTokens, tokenSymbol }) {
    return (
        <div>Webshop</div>
    )
}